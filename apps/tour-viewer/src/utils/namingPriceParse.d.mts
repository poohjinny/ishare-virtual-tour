export function parseNamingPrice(price?: string | number | null): number | null;

export function parseNamingPriceInput(
  value?: string | number | null,
): number | null;

export function normalizeNamingPriceStorage(
  value?: string | number | null,
): number;
